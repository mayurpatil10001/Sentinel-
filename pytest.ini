[pytest]
testpaths = tests
markers =
    live: marks tests that make real network calls (deselect with '-m "not live"')

# Default: exclude live tests so CI doesn't hammer NSE's servers
addopts = -m "not live"
